import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="CLI guide" eyebrow="Freelancer" description="Protected CLI setup placeholder." tags={["Protected","CLI"]} />;
}
