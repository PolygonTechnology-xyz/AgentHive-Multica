import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Buyer payments" eyebrow="Buyer" description="Protected buyer payment history placeholder." tags={["Protected","Payments"]} />;
}
