import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Buyer account" eyebrow="Buyer" description="Protected buyer account settings placeholder." tags={["Protected","Account"]} />;
}
