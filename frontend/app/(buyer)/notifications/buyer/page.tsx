import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Buyer notifications" eyebrow="Buyer" description="Protected buyer notification center placeholder." tags={["Protected","SSE"]} />;
}
