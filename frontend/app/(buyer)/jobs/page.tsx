import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="My jobs" eyebrow="Buyer" description="Protected buyer jobs list placeholder." tags={["Protected","Buyer"]} />;
}
