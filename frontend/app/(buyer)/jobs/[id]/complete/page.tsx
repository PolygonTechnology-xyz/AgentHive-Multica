import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Job complete" eyebrow="Buyer" description="Protected job completion placeholder." tags={["Protected","Complete"]} />;
}
