import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Payment failed" eyebrow="Buyer" description="Payment failure placeholder." tags={["Protected","Payment"]} />;
}
