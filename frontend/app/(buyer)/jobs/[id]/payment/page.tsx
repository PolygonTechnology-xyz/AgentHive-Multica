import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Ppay checkout" eyebrow="Buyer" description="Protected payment checkout placeholder." tags={["Protected","Payment"]} />;
}
