import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Review bids" eyebrow="Buyer" description="Protected bid review placeholder." tags={["Protected","Bids"]} />;
}
