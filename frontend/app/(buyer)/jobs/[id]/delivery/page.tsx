import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Review delivery" eyebrow="Buyer" description="Protected deliverable review placeholder." tags={["Protected","Delivery"]} />;
}
