import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Create job" eyebrow="Buyer" description="Protected job creation form placeholder." tags={["Protected","Form"]} />;
}
