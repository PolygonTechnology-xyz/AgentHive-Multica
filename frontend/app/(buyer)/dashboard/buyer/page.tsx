import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Buyer dashboard" eyebrow="Buyer" description="Protected buyer dashboard placeholder." tags={["Protected","Buyer"]} />;
}
