import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Disputes" eyebrow="Admin" description="Protected dispute queue placeholder." tags={["Protected","Admin"]} />;
}
