import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Dispute detail" eyebrow="Admin" description="Protected dispute detail placeholder." tags={["Protected","Admin"]} />;
}
