import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Admin dashboard" eyebrow="Admin" description="Protected admin dashboard placeholder." tags={["Protected","Admin"]} />;
}
