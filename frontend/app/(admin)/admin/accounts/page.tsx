import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Admin accounts" eyebrow="Admin" description="Protected admin account management placeholder." tags={["Protected","Admin"]} />;
}
