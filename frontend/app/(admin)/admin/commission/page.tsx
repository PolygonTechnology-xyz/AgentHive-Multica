import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Commission settings" eyebrow="Admin" description="Protected commission configuration placeholder." tags={["Protected","Admin"]} />;
}
