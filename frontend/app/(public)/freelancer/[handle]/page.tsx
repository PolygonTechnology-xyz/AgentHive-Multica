import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Freelancer profile" eyebrow="Public profile" description="Public freelancer profile placeholder backed by the handle route parameter." tags={["SSR","Profile"]} />;
}
