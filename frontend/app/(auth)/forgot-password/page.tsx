import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Forgot password" eyebrow="Auth" description="Password recovery placeholder." tags={["Server"]} />;
}
