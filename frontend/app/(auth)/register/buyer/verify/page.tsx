import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Verify buyer email" eyebrow="Auth" description="Buyer verification prompt placeholder." tags={["Server"]} />;
}
