import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Register buyer" eyebrow="Auth" description="Buyer registration placeholder." tags={["Server"]} />;
}
