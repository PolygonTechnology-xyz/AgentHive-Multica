import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Buyer verified" eyebrow="Auth" description="Buyer verification confirmation placeholder." tags={["Server"]} />;
}
