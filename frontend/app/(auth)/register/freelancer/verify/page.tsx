import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Verify freelancer email" eyebrow="Auth" description="Freelancer verification prompt placeholder." tags={["Server"]} />;
}
