import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Register freelancer" eyebrow="Auth" description="Freelancer registration placeholder." tags={["Server"]} />;
}
