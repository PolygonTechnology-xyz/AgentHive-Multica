import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Choose login role" eyebrow="Auth" description="Role selector placeholder for buyer, freelancer, and admin sign-in." tags={["Server"]} />;
}
