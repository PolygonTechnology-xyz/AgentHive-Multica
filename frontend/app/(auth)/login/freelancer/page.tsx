import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Freelancer login" eyebrow="Auth" description="Freelancer credential login placeholder." tags={["Server"]} />;
}
