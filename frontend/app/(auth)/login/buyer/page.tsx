import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function Page() {
  return <RoutePlaceholder title="Buyer login" eyebrow="Auth" description="Buyer credential login placeholder." tags={["Server"]} />;
}
